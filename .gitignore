# === Secrets — NEVER commit these ===
.env
.env.bak
.env.local
*.pem
*.key

# === Dependencies ===
node_modules/

# === Hardhat build artifacts ===
artifacts/
cache/
typechain-types/
coverage/
coverage.json

# === Logs & runtime noise ===
*.log
*.err
.hardhat-node.log
.hardhat-node.err
node.out.log
node.err.log
mind-dash.out.log
mind-dash.err.log
tunnel.log
tunnel.err

# === Local runtime state ===
.node.pid
deployments/
mind/state.json
mind/log.jsonl
mind/journal.md
mind/registry.json

# === Tracker caches / generated data ===
tracker/.coingecko-list.json
tracker/.market-cache.json
tracker/price-map.json
tracker/race-history.json

# === Scratch / verification scripts ===
_*.js
tracker/_*.js
.remote-url.txt

# === Large binaries ===
tools/*.exe
tools/*.bat

# === OS noise ===
Thumbs.db
.DS_Store
